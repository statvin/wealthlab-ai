# Copie para .env e ajuste conforme necessário.

# Caminho do cache SQLite do histórico de preços (camada de market data).
WEALTHLAB_CACHE_PATH=data/cache/market.sqlite

# Nível de log do pacote (DEBUG, INFO, WARNING, ERROR).
WEALTHLAB_LOG_LEVEL=INFO
